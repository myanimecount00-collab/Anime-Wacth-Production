name: animevault_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  cupertino_icons: ^1.0.8
  webview_flutter: ^4.13.0   # diperbarui dari ^4.8.0
  flutter_inappwebview: ^6.1.5
  video_player: ^2.10.0
  # Firebase
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0

  provider: ^6.1.2
  shared_preferences: ^2.2.3
  google_sign_in: ^6.3.0
  flutter_custom_tabs: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true